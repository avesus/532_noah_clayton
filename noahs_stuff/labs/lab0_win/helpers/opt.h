#ifndef _MY_OPT_H_
#define _MY_OPT_H_

#include <stdlib.h>
#include <stdio.h>
#include "config.h"


#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#define MIN(x, y) (((x) < (y)) ? (x) : (y))

unsigned int ulog2(unsigned int n);
		   

#endif
